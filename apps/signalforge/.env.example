# No environment variables or API keys are needed. Demo mode is the only runtime.
# Future public research adapter configuration is intentionally not consumed.
# Never place secrets in NEXT_PUBLIC_ variables.
