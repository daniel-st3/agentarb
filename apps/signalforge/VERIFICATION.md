# Verification record

Local verification, 2026-08-30. No deployment or push.

- SignalForge: 65 unit/API/boundary tests.
- Browser: 14 passing checks across desktop/mobile; 4 duplicate mobile geometry
  cases intentionally skipped because geometry runs cover 390/768/1024/1440 in
  the desktop browser project. The complete workflow runs in both projects.
- ESLint and TypeScript strict checks pass.
- Next.js 16.3.3 production build and local next start verified.
- npm dependency audit: zero reported vulnerabilities.
- ESLint is pinned to 9.39.5: the React plugin bundled by Next 16.3.3 fails
  with ESLint 10's removed context APIs. npm emits a v9 deprecation warning;
  lint itself passes. Upgrade this dev tool when the bundled plugin is compatible.
- Archived Python: 305 tests passed; three live marketplace tests intentionally
  not run for this research-product pivot. Ruff passes.
- Archived Agent Arbiter web: 253 tests pass, including parity and limiter tests.
- Archived golden corpus: 40/40 correct; zero unsafe false-allows.

Browser checks cover request → plan → Run → brief, independent-source labels,
both downloads, cheaper routes, unknown session recovery, session clearing,
reduced motion, safe loading/error states, and no horizontal overflow at four
widths. Main workflow checks report no console/page errors or external requests.

Screenshots are generated by npm run test:e2e in test-results/screenshots.
Captures use reduced motion for a stable static-equivalent portfolio artifact;
normal motion is exercised separately. The loading screenshot deliberately holds
a local test request and is not evidence of a slow provider or live research.

Known limitations: predefined fictional briefs, no real/public provider execution,
no durable hosted repository, no distributed limiter on the zero-I/O demo routes,
and no production deployment validation. These are documented, not hidden behind
live-research or production-operation claims.
