# SignalForge — 55-second recording

Use non-sensitive example text. Keep environment panels and developer consoles off screen. Never narrate simulated results as execution.

| Time | Screen/action | Narration |
|---|---|---|
| 0–8s | Objective Console; due diligence, $0.25, Most verified | “SignalForge turns an agent objective into a budget-constrained route—not an executed task.” |
| 8–18s | Decompose → compile; capability chain | “Groq can interpret the objective. Deterministic rules retain budget, capability and verification control.” |
| 18–28s | Selected/rejected providers; observed options | “These providers are simulated. These catalog options are timestamped and never called.” |
| 28–35s | Network; switch ES → FR | “One discovery and planning workflow, in three languages.” |
| 35–47s | Separate terminal: client-agent REST demo, new receipt filename | “An external client validates the deployed API contract and records a local decision.” |
| 47–55s | Unsafe fixture, another receipt path | “Unsafe contracts are refused. Neither decision executes services, pays or writes to a marketplace.” |

Preflight the client once, respect shared quotas, and use a fresh receipt path. Unsafe fixture exit 2 is expected. If Groq is unavailable, explicitly call the output local demo decomposition. Catalog counts are bounded observations, not total market size.
