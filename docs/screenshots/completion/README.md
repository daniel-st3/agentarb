# Final completion evidence

- `hero-en.png`, `hero-fr.png`, `mobile-fr.png`, `route-contract.png`: real local production-build browser captures. Route providers/output are explicitly simulated.
- `network-demo.png`: hermetic browser test with clearly labeled controlled fixtures, not live observations.
- `network-es-observed.png`: local production server backed by configured shared catalog cache; actual timestamps and source counts are visible. Observations are not service execution.
- `client-agent-recorded.png`: browser rendering of the verbatim terminal output from a separate client’s real REST request to the deployed planning API. It is labeled as a recorded run, not live telemetry. The corresponding readable artifact is `apps/signalforge/public/client-proof.html`.

Captured during the 2026-08-30 America/Bogota completion pass (UTC 2026-08-31). No secret/configuration panels, private objectives or credentials appear. Counts are bounded snapshots, not marketplace totals. Client receipts remain local and are not committed.
