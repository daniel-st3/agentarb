# Arbitrage Intelligence / 60-second demo

Record the preview, not an unapproved production promotion. Never show environment panels or secrets.

| Time | Action | Say |
|---|---|---|
| 0–8s | Hero: $1.20 → $0.45 → $0.75 | “SignalForge asks whether an agent’s fulfillment route makes economic sense. These figures are explicitly simulated.” |
| 8–18s | Open Arbitrage Lab; inspect extraction and verification-expensive rows | “The same deterministic engine compares competing routes and refuses negative spreads or missing capabilities.” |
| 18–30s | Choose policy-sensitive profile; move minimum margin from 25% to 90% | “Policy changes alter eligibility. Here, profitable becomes marginal. The reason and prior decision remain visible.” |
| 30–40s | Inspector waterfall, success scenario, payout sensitivity | “Conditional spread and risk-adjusted value are separate. Unknown inputs never become fake precision.” |
| 40–48s | Observed tab, then Network | “Supply catalogs are genuinely observed. No paid-task source has passed the gate, so demand is honestly empty.” |
| 48–60s | Developers → compile underwriting receipt | “Another agent can inspect the same result through REST or MCP. The receipt is auditable, not permission to execute.” |

Useful URLs: `/en/opportunities?mode=lab&id=lab:sensitive`, `/es/opportunities`, `/fr/opportunities`, `/developers/try`. Use the browser’s real download for the JSON receipt.
